/*#pragma once

#include "EntityList.h"

class CollisionManager{
private:
	Entity *ent;
	EntityList *list;
public:
	CollisionManager(Entity* ent, EntityList* list);
	~CollisionManager();
	void update();
};*/




